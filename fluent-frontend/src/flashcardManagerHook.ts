import { useState, useEffect } from 'react';
import { Flashcard } from './types';

export default function useFlashcardManager() {
  const [flashcards2, setFlascards2] = useState<Flashcard[] | null >(null);


  const flashcardManager = {

  };



  return ;//[flashcards2, fetchMoreFlashcards, getFlashcardManager];
}
  //const [flashcards2, flashcardManager] = useFlashcardManager();